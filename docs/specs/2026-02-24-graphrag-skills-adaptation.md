# Специфікація: Адаптація скіллів GraphRAG під проєкт Grynya

**Дата:** 2026-02-24
**Сесія інтерв'ю:** session_005 (GraphRAGSkillsReview)
**Автор:** Гриня (за результатами discovery-interview з користувачем)

---

## 1. Коротке Резюме

Доопрацювання двох скіллів — `graphrag-evaluation` та `graphrag-system-design` — з метою трансформації їх із generic-довідників у **вбудовані знання агента**, адаптовані під конкретну систему Grynya (FalkorDB, Docker, MCP, Cypher). Скілли мають працювати як "інтуїція" для будь-якого LLM-агента, що підключається до воркспейсу.

---

## 2. Опис Проблеми

Поточний стан скіллів:
- Написані **англійською мовою** (порушення мовного протоколу воркспейсу)
- Містять **generic контент** (Neo4j, Pinecone, LangChain, healthcare, finance) без прив'язки до нашого стеку
- **Не інтегровані** між собою та з `memory-manager`
- **Не адаптовані** під конкретну схему графа Grynya (`grynya-schema.md`)
- Generic ресурси потрапляють у контекст агента **цілком**, навіть коли потрібна лише частина інформації

Доведена цінність (PoC): аудит графа від 2026-02-24 підтвердив, що навіть у поточному generic-стані `graphrag-evaluation` дає **якісний результат аудиту** (оцінка 1.5/5.0 з конкретними рекомендаціями).

---

## 3. Критерії Успіху

| Критерій | Вимірювання |
|---|---|
| Агент-агностичність | Скілли працюють однаково для Claude, Gemini CLI, та інших LLM |
| Мовний протокол | SKILL.md та Grynya-specific ресурси — українською |
| Ізоляція контексту | Generic ресурси НЕ потрапляють у контекст агента автоматично |
| Адаптованість | Grynya-specific ресурси прив'язані до `grynya-schema.md`, FalkorDB, Cypher |
| Інтеграція | `memory-manager` SKILL.md посилається на graphrag-скілли як рекомендоване читання |
| Самодостатність | Агент, що прочитав скілл, розуміє як оцінити/покращити граф Grynya без додаткових інструкцій |

---

## 4. Портрети Користувачів

### Зараз: AI-агент (Claude / Gemini CLI)
- Читає скілли автоматично при підключенні до воркспейсу
- Використовує знання для якісної побудови та оцінки графа
- Не потребує пояснень щодо структури воркспейсу

### Перспектива: Нетехнічний користувач
- Клонує репозиторій, пише перший запит
- Агент під капотом використовує скілли для автоматичної побудови якісного графа
- Користувач навіть не знає про існування скіллів

---

## 5. Архітектура Доопрацювання

### 5.1 Структура файлів (цільова)

```
graphrag-evaluation/
├── SKILL.md                          # 🔄 Переписати українською, адаптувати під Grynya
├── resources/
│   ├── grynya-methodology.md         # 🆕 Grynya-specific: як оцінювати граф Grynya
│   ├── grynya-reasoning.md           # 🆕 Grynya-specific: патерни міркувань для графа пам'яті
│   ├── evaluators/
│   │   └── rubric_evaluation.json    # ✅ Залишити (оціночна рубрика — universal)
│   └── references/                   # 🆕 Субдиректорія для generic ресурсів
│       ├── methodology.md            # 📦 Перемістити сюди (generic)
│       └── reasoning-patterns.md     # 📦 Перемістити сюди (generic)

graphrag-system-design/
├── SKILL.md                          # 🔄 Переписати українською, адаптувати під Grynya
├── resources/
│   ├── grynya-stack.md               # 🆕 Grynya-specific: наш стек (FalkorDB/Docker/MCP/Cypher)
│   ├── grynya-patterns.md            # 🆕 Grynya-specific: Activity-Centric Memory Agent
│   ├── evaluators/
│   │   └── rubric_system_design.json # ✅ Залишити (universal)
│   └── references/                   # 🆕 Субдиректорія для generic ресурсів
│       ├── methodology.md            # 📦 Перемістити сюди (generic)
│       ├── technology-stacks.md      # 📦 Перемістити сюди (generic)
│       └── domain-patterns.md        # 📦 Перемістити сюди (generic)
```

### 5.2 Принцип ізоляції контексту

**Проблема:** Якщо agent читає `resources/technology-stacks.md` (264 рядки), він отримує ~16KB generic інформації, з якої релевантно менше 5%.

**Рішення:** Субдиректорія `references/`:
- Generic ресурси переміщені в `references/` — агент НЕ читає їх автоматично
- SKILL.md та Grynya-specific ресурси посилаються на `references/` ТІЛЬКИ як на "додаткове читання, якщо потрібна ширша перспектива"
- Grynya-specific ресурси містять усю необхідну інформацію для повсякденної роботи

### 5.3 Інтеграція з `memory-manager`

У `memory-manager/SKILL.md` додати секцію:

```markdown
## Рекомендоване Читання

> Перед першою роботою з графом рекомендується ознайомитися з:
> - `.agent/skills/graphrag-evaluation/SKILL.md` — як оцінювати якість графа
> - `.agent/skills/graphrag-system-design/SKILL.md` — як проектувати зміни до схеми графа
>
> Ці скілли містять адаптовану експертизу з графових баз даних конкретно для проєкту Grynya.
```

---

## 6. Функціональні Вимоги

### P0 — Обов'язково

| # | Вимога | Критерії приймання |
|---|---|---|
| 1 | **Переписати SKILL.md обох скіллів українською** | Весь текст українською, крім code blocks та технічних термінів |
| 2 | **Створити `grynya-methodology.md`** для evaluation | Містить: як оцінювати якість графа Grynya, конкретні Cypher-запити для перевірки, прив'язка до `grynya-schema.md` |
| 3 | **Створити `grynya-stack.md`** для system-design | Містить: опис нашого стеку (FalkorDB, Docker, MCP bridge, Cypher), його можливості та обмеження |
| 4 | **Створити `grynya-patterns.md`** для system-design | Містить: патерни Activity-Centric Memory Agent, адаптовані з domain-patterns секції Enterprise |
| 5 | **Створити `grynya-reasoning.md`** для evaluation | Містить: патерни міркувань адаптовані під граф пам'яті (temporal, session-based, entity-linking) |
| 6 | **Перемістити generic ресурси в `references/`** | Файли переміщені, посилання в SKILL.md оновлені |
| 7 | **Додати cross-reference в `memory-manager`** | Секція "Рекомендоване читання" додана в SKILL.md memory-manager |

### P1 — Бажано

| # | Вимога | Критерії приймання |
|---|---|---|
| 8 | **Адаптувати workflow/чеклісти в SKILL.md** | Кроки workflow адаптовані під Grynya (замість generic "define your KG" → "перевір grynya-schema.md") |
| 9 | **Адаптувати output templates** | Шаблони звітів спрощені під контекст Grynya (видалити нерелевантні секції типу "HIPAA compliance") |
| 10 | **Переклад evaluators JSON** | Рубрики оцінки залишити англійською (JSON), але додати коментарі українською |

### P2 — Було б чудово

| # | Вимога | Критерії приймання |
|---|---|---|
| 11 | **Cross-reference між evaluation та system-design** | Кожен скілл знає про інший і посилається де доречно |
| 12 | **Секція "Поточний стан Grynya"** в Grynya-specific ресурсах | Коротке summary поточної архітектури для нового агента що вперше підключився |

---

## 7. Нефункціональні Вимоги

- **Розмір Grynya-specific ресурсів:** кожен файл < 200 рядків (щоб не перевантажувати контекст)
- **Агент-агностичність:** жодних посилань на конкретний LLM або IDE (ні Claude, ні Gemini)
- **Мова коду:** англійська (Cypher-запити, назви файлів, JSON ключі)
- **Мова документації:** українська (описи, пояснення, коментарі)

---

## 8. Що "Поза Рамками"

| Що НЕ входить | Чому |
|---|---|
| Імплементація рекомендацій аудиту (індекси, entity resolution, RAG-шар) | Це окремий проєкт, не доопрацювання скіллів |
| Створення runbook'ів з виконуваними інструкціями | Буде зроблено окремо, в наступних ітераціях |
| Інтеграція Gemini CLI | Окремий етап після завершення роботи зі скіллами |
| Переписування generic ресурсів | Залишаються як є, тільки переміщуються в `references/` |
| Переклад generic ресурсів на українську | Вони reference-only, залишаються англійською |

---

## 9. Відкриті Питання

| # | Питання | Коли вирішувати |
|---|---|---|
| 1 | Чи потрібен окремий скілл для "operational runbooks"? | Наступне інтерв'ю |
| 2 | Як именно CLI-агенти будуть інтегровані в мережу? | Після завершення роботи зі скіллами |
| 3 | Яка конкретна структура буде у `grynya-methodology.md`? | На етапі імплементації (обговорення в процесі) |
| 4 | Чи потрібна окрема рубрика оцінки для Grynya (замість generic)? | На етапі імплементації |

---

## Додаток: Результати Discovery Interview

**Сесія:** session_005 (2026-02-24, 12:37 — 14:24)
**Кількість ітерацій:** 5 feedback-response циклів
**Ключові рішення:**

1. **Варіант В** — повна кастомізація скіллів під Grynya (не просто переклад)
2. **Два окремих скілли** — evaluation та system-design залишаються роздільними
3. **Дворівневі ресурси** — Grynya-specific на першому рівні, generic references на другому (ізольовані)
4. **Напівавтоматична активація** — через cross-reference з `memory-manager` SKILL.md
5. **Агент-агностичність** — скілли для будь-якого LLM, не прив'язані до Claude/Gemini
6. **Рекомендації аудиту ≠ план дій** — аудит підтвердив цінність скілла, але план дій будується окремо
7. **Long-term vision** — нетехнічні користувачі, мультиагентна архітектура, self-evolving graph

**Proof of Concept:** Аудит графа Grynya (audit_2026-02-24_graphrag_evaluation.md) — оцінка 1.5/5.0, 10 конкретних слабкостей, пріоритезовані рекомендації.
