---
name: graphrag-evaluation
description: Оцінка якості графа пам'яті Grynya (та GraphRAG систем загалом). Робочий процес покриває цілісність графа (KG completeness), якість пошуку, правильність відповідей та запобігання галюцинаціям.
---

# Оцінка GraphRAG (GraphRAG Evaluation)

Використовуй цю навичку, коли користувач просить оцінити якість графа пам'яті (GraphRAG системи), виміряти показники (metrics), знайти розриви в хронології, перевірити правильність відповідей або зменшити галюцинації. Навичка містить фреймворки оцінки та методи тестування, адаптовані спеціально для графа Grynya (з можливістю застосування до інших GraphRAG-архітектур).

## Важливі ресурси
- [Методологія Grynya](resources/grynya-methodology.md): Конкретні Cypher-запити для перевірки цілісності графа та метрики здоров'я.
- [Патерни міркувань](resources/grynya-reasoning.md): Патерни оцінки сесій, хронології та самоаналізу агента.
- *Загальні теоретичні ресурси*: `resources/references/methodology.md` та `resources/references/reasoning-patterns.md`.

## Робочий процес (Workflow)

**КРОК 1: Визначення цілей оцінки (Scoping)**
Перш ніж запускати запити, визнач, ЩО саме ми оцінюємо:
1.  **Якість графа (KG Quality):** Цілісність сутностей, зв'язків та протоколів (Next, Тріада).
2.  **Якість пошуку (Retrieval):** Чи знаходить агент правильні сесії через сутності?
3.  **Правильність відповіді (Generation):** Наскільки точно відповіді агента використовують знання з графа.

*Спирайся на `resources/grynya-methodology.md` для узгодження метрик.*

**КРОК 2: Базова структурна перевірка (Health Check)**
Використай Cypher-запити з `grynya-methodology.md` (Чек-лист оцінки), щоб перевірити:
1.  Цілісність хронологічного ланцюга (`[:NEXT]`).
2.  Наявність `LAST_EVENT`.
3.  Правило Тріади (кожен `Response` має `full_text` та пов'язаний `Analysis`).
4.  Відсутність "сиріт" (Orphan Entities) та дублікатів.

**КРОК 3: Оцінка патернів міркувань (Reasoning Patterns Validation)**
Через `resources/grynya-reasoning.md`, проаналізуй як агент міркує над складними питаннями:
1.  **Темпоральний Трейсинг:** Чи коректна хронологія всередині обраної сесії?
2.  **Рефлексивний Аналіз:** Проаналізуй вузли `Analysis` на кількість знайдених помилок та сформованих уроків.

**КРОК 4: Оцінка відповідей та пошуку (Response & Retrieval Evaluation)**
Якщо потрібно оцінити здатність до генерації відповіді:
1. Згенеруй багатоетапні запити (2-hop, 3-hop) через спільні сутності (Cross-Session).
2. Оціни: 
   - Обґрунтованість: чи спирається `Response` виключно на `Entity`/вузли графа?
   - Галюцинації: чи не вигадує агент зв'язки, яких немає у БД.
3. Використай [Rubric Evaluation](resources/evaluators/rubric_evaluation.json) для стандартизованого оцінювання від 0 до 5.

**КРОК 5: Формування звіту (Reporting)**
Збери отримані результати та сформуй звіт (див. "Формат виводу" нижче). Запропонуй конкретні дії для виправлення структурних помилок (наприклад, Cypher-запити для відновлення розірваних зв'язків).

> [!CAUTION]
> **КРИТИЧНЕ ПРАВИЛО: ЖОДНОГО АВТОНОМНОГО ВИДАЛЕННЯ (NO AUTONOMOUS DELETION)**
> Під час оцінки графа або усунення проблем **НІКОЛИ** не використовуй деструктивні команди (наприклад, `DETACH DELETE` або заміну даних) самостійно без ПРЯМОГО дозволу користувача. Завжди спочатку пропонуй Cypher-код користувачеві як гіпотезу у своєму звіті.
> Крім того, перед тим як визначати вузли як "сироти" або "помилкові", переконайся у правильності напрямку зв'язків (векторності) та використовуй жорстку валідацію через заперечення (`NOT (паттерн)`), уникаючи хибнопозитивних результатів через особливості Cypher (`OPTIONAL MATCH + IS NULL`).

---

## Формат виводу звіту про оцінку

Під час генерації остаточного звіту використовуй наступну структуру:

```markdown
# Звіт про оцінку графа [Назва Системи/Сесії]

## 1. Підсумок результатів 
- **Основні знахідки:** [Резюме 2-3 реченнями]
- **Рівень здоров'я:** [🟢 Здоровий | 🟡 Потребує уваги | 🔴 Критичний]
- **Загальна кількість вузлів/зв'язків:** [Статистика з бази]

## 2. Структурна цілісність (Health Check)
| Метрика | Значення/Статус |
| :--- | :--- |
| Цілісність ланцюга `[:NEXT]` | [Значення, напр., 100% або список розірваних сесій] |
| Правило Тріади (Response + Analysis) | [Статус] |
| Entity Resolution (Дублікати/Сироти) | [Опис] |

## 3. Оцінка Міркувань (Reasoning Quality)
- **Аналіз `[:ANALYZES]`:** [Як часто агент знаходить помилки в своїх відповідях]
- **Оцінка темпоральності:** [Чи коректно відстежуються сесії/дати]

## 4. Виявлені Галюцинації / Відхилення
- [Приклад: Використання вигаданого ID сутності]
- [Приклад: Відповідь не збережена у `full_text`]

## 5. Плани дій (Рекомендації)
1. **[Рекомендація 1]:** [Детальний опис]
   - *Cypher-запит для виправлення:* `[Код]`
2. **[Рекомендація 2]:** [Детальний опис]
```

## Інтеграція з Memory Manager
Цього агента та цю навичку можна і потрібно викликати після значних оновлень графа. Наприклад, якщо користувач застосовує `/ss` (Session Summary), він може перед цим запросити GraphRAG Evaluation для виявлення структурних помилок поточної сесії. У такому разі використовуй `mcp_falkordb_query_graph` для перевірок БД.
